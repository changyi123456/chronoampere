# 《電的時光旅人 CHRONOAMPERE》優化評估報告

評估日期：2026-06-10 ｜ 範圍：`3D/` 全部源碼（physics.ts、HubScene、lab.tsx、六關 Rooms、script.ts、Hud、RoomShell）
目標：3A 級美學 × 海龜湯推理沉浸 × 真實實驗模擬。整體架構允許大改。

---

## 總評

現有基礎相當扎實：純函式物理引擎、固定時間步、多重條件過關、bootstrap 悖論劇情骨架都已成形。但距離「3A 美學 + 海龜湯沉浸」有三個結構性落差：

1. **渲染缺後製管線**——README 宣稱「發光泛光」，但專案沒有安裝 `@react-three/postprocessing`，全靠 `toneMapped={false}` 假裝發光，這是與 3A 質感差距最大的單一因素。
2. **六關共用同一個「現代木桌實驗室」**——劇情是穿越六個年代，場景卻完全沒有年代感，敘事與視覺脫鉤。
3. **海龜湯只是包裝**——目前的推理體驗實際上是「6 題選擇題 + 1 題四選一」，玩家從未真正「提問、假設、被否定、修正」，海龜湯的核心循環不存在。

以下分兩軸詳評，文末附優先級路線圖。

---

## 第一軸：物件設計、美學與玩家體驗

### 1.1 渲染管線（P0，效益最大）

| 現況 | 問題 | 建議 |
|---|---|---|
| 無 EffectComposer | emissive 物件不會真正泛光，夜景 hub 顯得「平」 | 安裝 `@react-three/postprocessing`：`Bloom`（luminanceThreshold≈1，配合現有 toneMapped:false 物件自然分層）、`Vignette`、`N8AO`、輕量 `ChromaticAberration`（穿越隧道時動態加強） |
| `dpr=[1,2]` 固定 | 手機高負載 | 加 `PerformanceMonitor` 動態降 dpr；提供低/中/高畫質設定 |
| 燈泡/門用 pointLight 堆疊 | 多光源開銷大、陰影不一致 | 燈光預算管理：每場景 ≤3 個投影光源，其餘改 emissive+bloom 表現 |

### 1.2 場景美術：六個年代應該是六個世界（P0）

`RoomShell` 目前是統一的米白牆 + 木桌 + 攝影棚光，與「1827 歐姆書房」「1897 卡文迪西實驗室」毫無關聯。建議把 RoomShell 改為**資料驅動的年代主題系統**：

```ts
interface EraTheme {
  ambient: string      // 色溫：1827 燭光琥珀 / 1897 早期電燈青白 / 1885 工業煤氣
  props: PropSet       // 年代道具：羽毛筆手稿、黃銅儀器、蒸汽管線、木製電報機
  skybox/window: ...   // 窗外景：德國小鎮雪景、劍橋庭院、城市夜景
  soundscape: ...      // 壁爐劈啪、懷錶滴答、遠處馬車、蒸汽嘶鳴
}
```

- 歐姆書房：燭光 + 暖色 vignette、桌上手稿與墨水瓶、窗外飄雪。
- 湯姆森/布勞恩：陰極射線的青綠輝光成為房間主光源（這本身就是該年代最美的視覺記憶點）。
- 法拉第：皇家研究院地下室，磁力線用 `Line`/shader 在空間中可視化。
- 每間房藏一張**實體紙條道具**（可走近檢視、與殘頁 UI 連動）→ 美術直接服務推理敘事。

### 1.3 物件造型（P1）

`lab.tsx` 元件庫方向正確（黃銅/陶瓷/銅線材質分明），但全是 primitive 拼裝：

- **優先替換主角級物件**：陰極射線管（玻璃折射用 `MeshTransmissionMaterial`，drei 內建，效果驚人且零美術成本）、發電機、變壓器鐵芯。次要物件（接線柱、電阻）保留 primitive 即可。
- 時光之門的「漩渦」目前是 3 個旋轉 torus → 改成**單面 shader 漩渦**（極座標 UV 旋轉 + noise + fresnel），這是 portal 的標準做法，質感提升一個世代。
- 導線 `Lead` 已用 CatmullRom tube，很好；可加 `CurrentFlow` 式的流動光點貼在導線上（依電流大小調速度與密度）→ 抽象電流變成可見實體，兼顧教學。
- `@react-three/rapier` 已安裝但完全未使用：要嘛用它做可拾取/掉落的道具互動（撿起磁鐵、紙條），要嘛移除省 bundle。

### 1.4 HTML 標籤是沉浸殺手（P1）

全遊戲大量使用 drei `<Html>` 做標籤、對話泡泡、儀表文字。問題：不受 bloom/fog/景深影響、縮放時跳動、白底標籤與深空調性衝突。建議：

- 世界內文字改 `<Text>`（troika SDF，支援中文需提供字型子集）。
- 儀表讀數做成**диegetic UI**：數碼管/Nixie 管風格的 3D 顯示屏，直接長在儀器上（年代感 + 沉浸雙贏）。
- HUD 保留 2D，但對話框、任務日誌統一視覺語言（目前 Hud.tsx 516 行，建議拆成 Dialogue / QuestLog / Oscilloscope / QuizModal 四個元件）。

### 1.5 玩家與鏡頭手感（P1）

- 無人機移動是瞬時速度，無加速/慣性/轉向傾斜（banking）。加二階阻尼（`v += (target-v)*k*dt`）+ 朝移動方向轉向 + 傾斜 10–15°，手感立刻「貴」起來。
- 鏡頭是硬 lerp 跟隨。建議 spring-arm + 進門時 FOV kick + 過關瞬間輕微 screen shake / 慢動作 0.3s（juice）。
- 進關目前是直接切場景。已有隧道動畫的話，確保「門 → 隧道 → 年代房間淡入 + 年代字卡（1827・德國科隆）」一氣呵成；字卡是最便宜的電影感。

### 1.6 海龜湯體驗重構（P0，與美學同等重要）

現況：每關 1 題 MCQ → 殘頁；結局 4 選 1。這是測驗，不是海龜湯。海龜湯的本質是**玩家主動提問 + 主持人只答是/否/無關 + 假設逐步收斂**。建議架構：

1. **提問系統**：殘頁集到 ≥2 張後，hub 解鎖「向 AMP 提問」。提供問題庫（每張殘頁解鎖 3–5 個可問問題，如「紙條的墨水是過去的嗎？」「六位科學家見過寫紙條的人嗎？」），AMP 只回「是 / 否 / 與真相無關」。免費問題有限（例如 10 次），製造珍惜感。
2. **證據牆（evidence board）**：把 6 張殘頁 + 問答記錄釘在 hub 的一面 3D 板上，玩家可拖線連接線索（紅線連結板是推理題材的招牌視覺）。每連對一組（如「Ω 符號 × 1881 年命名」→「紙條含有未來資訊」）就點亮一個中間結論。
3. **紅鯡魚**：目前 4 個結局選項太好猜（B 明顯最長最詳細）。加入 1–2 條誤導線索（導師的可疑行蹤、第七張看似存在的殘頁），且結局選項等長改寫。
4. **多段結局**：答錯不該只是「再選一次」——給「時間軸劣化」的替代結局（迴圈未閉合的世界），答對才有完整 reveal。提高重玩價值與情緒重量。
5. **環境敘事前置**：每間房的實體紙條道具（§1.2）讓玩家在解物理題時就「親手把紙條留在桌上」而不自知（例如過關動畫中無人機自動放下一張紙）——結局 reveal 時回放這六個瞬間，玩家會起雞皮疙瘩。這是 bootstrap 悖論最強的演出法。

### 1.7 其他 UX（P2）

- 進度無存檔：`localStorage` 持久化 solved/殘頁/問答記錄（注意：部署為 web artifact 時需改用記憶體存檔）。
- 過關後房間應留下「已重現」的視覺狀態（燈長亮、發電機持續轉），hub 六道門逐一變綠的儀式感已有，很好。
- 音訊：procedural ambient 已有，補上互動音（旋鈕喀噠、燈絲嗡鳴隨溫度升頻、保險絲熔斷爆裂）與**分層 BGM**（每集一張殘頁加一層樂器）。

---

## 第二軸：真實實驗模擬評估

物理引擎整體**正確且乾淨**（Boris 旋轉保 |v|、RL/熱模型一階 ODE、CRT 類平拋推導無誤）。以下按關卡列「正確性 → 真實性升級 → 探究性升級」：

### 2.1 Lamp（歐姆串聯）

- ✅ 穩態電流、焦耳熱、牛頓冷卻、熔斷判定皆正確。
- ⚠️ **燈絲電阻固定 6Ω 不符真實**：鎢絲 R(T) = R₀(1+αΔT)，α≈4.5×10⁻³/K，冷熱阻差可達 10 倍。加入後燈泡變成非線性元件——這正是「歐姆定律適用條件」最好的教材（歐姆定律在定溫下才成立），且會產生有趣的動態：開機湧浪電流 → 升溫 → 電流回落。模擬只需把 `lampCurrent` 改為依當前 T 計算，迭代即可。
- ⚠️ Tburn=120°C 是遊戲縮放值（真實鎢絲工作 ~2700K、熔點 3695K）。可接受，但建議 HUD 註明「示意溫標」或提供「真實尺度模式」。
- 📜 史實：1827 年沒有電流表（檢流計 1820 年才出現，刻度安培計更晚）；歐姆實際用熱電偶電源 + 扭秤。把 DialMeter 換成**扭秤檢流計**（指針懸絲扭轉）既正確又有年代美術價值。

### 2.2 Split（克希何夫並聯）

- ❌ **內阻不一致**：`splitCurrents` 用 i = ε/R 完全忽略電池內阻，但 Lamp 關有 rInternal=0.5Ω。同一顆「電池」物件行為不同，學生若兩關對照會困惑。建議統一加入內阻：`V_bus = ε − r·I_total`，需迭代或解析解（一行公式可解）。這同時讓「多接一條支路 → 端電壓微降 → 其他支路電流微降」的真實現象出現，是並聯教學的高階亮點。
- ✅ 保險絲 I²t 熱模型合理。可補：保險絲熔斷時間與過載倍數的反時限特性曲線（真實保險絲規格書就是這條曲線），作為進階讀數。

### 2.3 Cyclo（湯姆森 e/m）

- ✅ v=√(2eV/m)、r=mv/eB、e/m=2V/(B²r²) 全對；coilK≈7.8×10⁻⁴ T/A 與真實亥姆霍茲線圈（N≈130、R≈0.15 m）同量級，數值誠實。
- 💡 **最該加測量不確定度的一關**：真實 e/m 實驗的靈魂是「讀半徑有誤差 → 多組 (V, I, r) → 取平均/作圖求斜率」。建議：束圓半徑讀值加 ±2% 視覺噪聲，要求玩家記錄 3 組數據、遊戲自動畫 2V vs B²r² 圖、由斜率得 e/m——這正是 COV/CER 探究流程，而且湯姆森本人當年也只測到量級正確。過關條件從「調到指定值」升級為「測得 e/m 在 1.76×10¹¹ 的 ±10% 內」。
- ⚠️ 確認電子束軌跡是用 `stepLorentz` 即時積分繪製（而非直接畫半徑 r 的圓），調 I 時束圓應連續變形——這是「真實時間演化」承諾的兌現點。

### 2.4 Maglock（布勞恩 CRT）

- ✅ 平行板類平拋 + 出板直線 + 磁偏轉疊加，公式正確；「電場先偏 −3 cm、再用磁場反拉到 +1 cm」的三耦合設計是全遊戲最好的一關。
- 💡 把**湯姆森平衡法**做成明確的中間步驟：先調 B 使 y=0（E=vB 平衡）→ 遊戲顯示「由平衡條件測得 v = E/B = …」→ 再對照 √(2eVa/m)。歷史上這正是測 v 的方法，目前的目標設定（−3cm/+1cm）反而繞過了它。
- 📐 忽略邊緣場（fringing field）合理，可在「實驗筆記」中註明假設——培養模型意識。

### 2.5 Dynamo（法拉第發電機）

- ✅ ε(t)=NBAω·sin(ωt) 正確，flux 與 emf 相位差 90° 的數據都已備好——**務必在示波器同屏畫 φ(t) 與 ε(t) 兩條曲線**，「emf 最大時磁通恰為零」是學生最大迷思點。
- 💡 加負載與冷次定律：接上電阻後 I=ε/R 產生反向力矩 τ=NIAB·sin(θ)，維持 ω 需要更大輸入功率。若把 ω 改成「玩家持續搖手柄（按住/搖桿）對抗反力矩」，能量守恆變成體感——這是 3A 級的「物理手感」。
- 📜 區分集電環（AC）與整流子（DC）可做成房間裡的可切換道具，對應課綱發電機/電動機對比。

### 2.6 Xfmr（變壓器）

- ✅ 理想變壓比正確。
- 💡 目前次級開路、只看電壓，資訊量偏低。建議：接負載後顯示 I₁/I₂ = N₂/N₁（功率守恆）；提供「理想 ↔ 真實」切換：耦合係數 k、鐵損（渦流 + 磁滯，效率 ~95%）、負載越重壓降越明顯。「為什麼鐵芯要疊片」可做成一個檢視互動。
- ⚠️ `omega = 2π·0.8` 是顯示用頻率，與「市電」敘述不符（50/60 Hz 畫不出來是合理妥協），在 HUD 註記「波形已放慢 75 倍」即可——誠實標注勝過默默縮放。

### 2.7 跨關卡的系統性建議

1. **單位制不一致**：Ch2 兩關用真實 SI（電子質量、特斯拉），Ch1/Ch3 用玩具縮放值。建議全面提供「教學尺度 / 真實尺度」雙模式，或至少在 HUD 標明縮放。
2. **積分器**：Euler 對目前 τ≫dt 的系統穩定無虞，但 RL 與熱方程有解析解（指數鬆弛），可直接用 exact update `i += (i_∞−i)(1−e^{−dt/τ})`——零誤差且永不發散，比升級 RK4 更便宜。
3. **從「調到目標值」到「真探究」**：六關目前都是 target-matching。挑 2–3 關（建議 cyclo、lamp）升級為「收集數據 → 作圖 → 擬合 → 回答」的 COV 流程，並把操作 log（每次調參、時間戳）寫入可下載的 log file——同時服務你的研究數據收集（行為層證據）。
4. **守恆量自檢**：physics.ts 註解寫了「能量/守恆可檢查」但未實作。加 debug 面板顯示 Boris 的 |v| 漂移、RL 能量帳——對外是品質保證，對內是回歸測試。

---

## 優先級路線圖

| 優先 | 項目 | 工作量 | 效益 |
|---|---|---|---|
| **P0** | postprocessing（Bloom/Vignette/AO） | 小 | 視覺質感最大單點提升 |
| **P0** | 海龜湯提問系統 + 證據牆 | 中 | 把「測驗」變成「推理」，核心體驗成立 |
| **P0** | 六關年代化場景（EraTheme 資料驅動） | 大 | 敘事與視覺合一 |
| **P1** | 內阻統一、燈絲 R(T)、發電機負載/冷次 | 小–中 | 物理真實性與教學深度 |
| **P1** | e/m 關測量不確定度 + 數據作圖過關 | 中 | 探究性（COV）標竿關卡 |
| **P1** | 無人機手感 + 鏡頭 juice + 年代字卡 | 小 | 體感「貴」起來 |
| **P1** | Html → diegetic 3D 儀表/Text；Hud 拆分 | 中 | 沉浸 + 可維護性 |
| **P2** | 主角級物件 GLTF/Transmission 材質、portal shader | 中 | 美術上限 |
| **P2** | 存檔、分層 BGM、互動音效、多結局 | 中 | 完成度與重玩價值 |
| **P2** | 操作 log 下載（研究數據） | 小 | 研究價值 |

**建議第一刀**：P0 三項中先做 postprocessing（半天內可見效），再做海龜湯提問系統（它決定這款遊戲「是什麼」），年代化場景最後做（工作量最大，但前兩項完成後它的規格會更清楚）。
