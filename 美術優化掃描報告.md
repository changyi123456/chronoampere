# 《電的時光旅人》遊戲物件美術掃描報告

掃描範圍：HubScene、lab.tsx 器材庫、六關 Rooms、RoomShell ｜ 依 physics-rpg-3d 視覺準則（深空科幻 / 低環境光 + 聚焦色光 / emissive 發光物件 + Bloom）
原則：以下配方**全部不需外部美術資源**，只用 primitive 組合、程序式 CanvasTexture、drei 內建元件——可直接寫進現有程式碼。

---

## 總診斷

目前美術的「天花板」卡在三件事：

1. **所有物件都是裸 primitive**——方盒就是方盒，沒有倒角、沒有材質紋理。3A 感的 80% 來自「邊緣與表面的微小細節」。
2. **重複元素沒有做出節奏**——刻度、線圈疊片、地板環刻這類「重複小物」最適合 instancing，做出來密度感立刻翻倍。
3. **玻璃都是假玻璃**——`transparent opacity={0.1}` 的玻璃沒有折射。drei 的 `MeshTransmissionMaterial` 一行就能換成真折射玻璃，是本報告 CP 值最高的單點升級。

---

## A. 通用技術配方（先建這三個工具，後面全部吃得到）

### A1. 程序式 CanvasTexture 工廠（新檔 `src/components/textures.ts`）

用 2D canvas 畫貼圖、`new THREE.CanvasTexture(canvas)` 餵給材質。建議做四張：

| 貼圖 | 畫法 | 用在 |
|---|---|---|
| `woodTexture(tone)` | 縱向條紋 + 噪點 + 少量深色木節 | 全部桌面、儀器底座（每個年代傳入 ERAS.desk 色調） |
| `blackboardTexture(formulas)` | 深綠底 + 白色手寫體公式（`ctx.fillText` 物理式：V=IR、ε=−dφ/dt…） | 各房間後牆掛一塊黑板——**年代敘事 + 物理教學雙贏，本報告最推薦的一項** |
| `paperTexture()` | 米黃底 + 咖啡漬橢圓 + 折痕線 | 神秘紙條（取代純色） |
| `brushedMetal()` | 水平細線 + 噪點 | 儀表面板、變壓器鐵芯 |

### A2. drei 即戰力元件（已安裝，零成本）

- `<RoundedBox>`：取代所有家具/儀器外殼的 `boxGeometry`，倒角立刻去掉「程式感」。
- `<MeshTransmissionMaterial>`：真折射玻璃（燈泡、e/m 球管、CRT 管）。參數起手式：`thickness={0.4} roughness={0.08} transmission={1} ior={1.5} chromaticAberration={0.03}`。
- `<Trail>`：掛在無人機後面一行就有拖尾光帶——hub 移動手感的視覺回饋。
- `<Instances>`：刻度、疊片、書架書本等重複物。
- `<Sparkles>`：已用於 hub；建議縮小尺度後用於「電刷接觸點火花」「過關粒子」。

### A3. Emissive 強度分級制（配合現有 Bloom threshold=1）

目前各物件 emissiveIntensity 隨手填（0.4～3 不等），建議定義常數分級，視覺層次立刻穩定：

```ts
export const GLOW = { hero: 2.2, focus: 1.4, accent: 0.8, ambient: 0.3 }
// hero：燈絲、傳送門漩渦、燭焰（畫面唯一主角）
// focus：目標環、儀表指針、束流
// accent：邊條、刻度環、AMP 光環
// ambient：待機儀器的微光
```

---

## B. Hub（時空樞紐）物件逐一掃描

| 物件 | 現況 | 升級配方 |
|---|---|---|
| 時光之門拱框 | 單一 torus | 改「分段石碑環」：12 段梯形塊用 `<Instances>` 沿圓排列，段間留縫透出漩渦光；頂部加一塊楔石（keystone）刻發光符文（小 plane + canvas 符號貼圖）。完成的門符文轉綠 |
| 門底座 | 兩層 cylinder | 加三階同心圓台階 + 階沿發光細環（ringGeometry, accent 級）；底座周圍地板加radial 漸層光暈貼圖（canvas 圓形漸層、AdditiveBlending） |
| 傳送門漩渦 | shader ✓（已是亮點） | 加「被吸入的粒子流」：30 顆小 Points 沿螺線朝盤面收束（uTime 驅動），靠近門時加速 |
| ChronoCore | 三環陀螺 ✓ | 核心外包一層 `MeshTransmissionMaterial` 水晶殼（icosahedron 放大 1.15 倍），環上加 `<Instances>` 小發光節點（每環 8 顆），轉動時像數據在流 |
| 玩家無人機 | icosahedron + 光環 | ① `<Trail width={0.4} length={6} color="#5eead4">` 拖尾；② 機身改複合體：主殼（RoundedBox 壓扁）+ 前視窗（transmission 小球）+ 左右推進莢艙（capsule×2，尾端 emissive 噴口，速度越快越亮）；③ 速度快時光環傾斜度加大 |
| AMP | 球體 + 雙眼 ✓ 已可愛 | ① 眨眼：每 3–5 秒把眼睛 scale.y 壓 0.1、80ms 回彈；② 說話時天線頂球閃爍；③ 身後加兩條垂墜小機械臂（cylinder 鏈）增加輪廓辨識度 |
| 反射地板 | ✓ | 加「電路紋路」：canvas 畫同心圓 + 放射狀細線 + 六個門位的節點，作為 emissiveMap 貼在地板上（ambient 級微光）——地板從「黑鏡」變「時光電路盤」，主題直接呼應電磁學 |
| 星空背景 | Stars ✓ | 加遠景星雲：超大球體內面貼 canvas 漸層雲霧（兩三團紫/青色 radial gradient），緩慢自轉——深度感大增，成本一張 512px 貼圖 |

## C. 實驗器材庫（lab.tsx）逐一掃描

| 器材 | 現況 | 升級配方 |
|---|---|---|
| BatteryPack | 深色方盒 | **改成伏打堆（Voltaic Pile）**：鋅/銅圓盤交錯堆疊（`<Instances>` 兩色 cylinder ×16）+ 玻璃罩。1827 年根本沒有「電池盒」——這一改年代感和物理史正確性同時到位 |
| DialMeter | 平圓筒 + 白面 | ① 玻璃表蒙：半球 transmission 罩在表面上；② 黃銅斜面外圈（torus 半徑漸縮）；③ 刻度：`<Instances>` 30 條細白塊沿弧排列；④ 指針軸心加一顆黃銅螺絲頭。四步之後它就是古董檢流計 |
| Rheostat | ✓ 結構好 | 瓷管加「繞線紋」：canvas 畫等距深色細線當 bump/color 貼圖；滑塊頂端加木質握柄（小球） |
| BulbLamp | 玻璃假透明 | ① 玻殼換 `MeshTransmissionMaterial`；② 螺口加三圈 torus 疊成螺紋；③ 玻璃內加支撐絲（兩根細 cylinder 撐住燈絲 torus）——點亮時從折射玻璃裡看到發光燈絲，是全遊戲最該「貴」的瞬間 |
| CoilHelix | ✓（程序螺線是亮點） | 銅色加清漆感：`roughness 0.25、envMapIntensity 1.5`；通電時用 `<Instances>` 小光點沿螺線跑（替代 emissive 全亮，更像電流） |
| BarMagnet | 紅藍方塊 | 改馬蹄形：半 torus + 兩段直柱；極面加銀色端蓋；N/S 用刻字 plane 而非白底標籤 |
| SupplyBox | 方盒 | RoundedBox + 正面儀表小圓窗（迷你 DialMeter）+ 兩顆旋鈕浮雕 + 散熱柵（`<Instances>` 細條） |
| Lead 導線 | ✓ | 兩端加香蕉插頭（小 cylinder + 球頭，黃銅色）；和接線柱真正「插」在一起 |

## D. 六關房間專屬道具（每關 2–3 件就夠，別貪多）

| 關卡 | 建議道具（全 primitive 可組） |
|---|---|
| lamp 1827 書房 | 書堆（RoundedBox×5 疊歪）、羽毛筆+墨水瓶（cone+cylinder）、黑板寫 V=IR 草稿 |
| split 1845 書齋 | 壁掛油燈、捲軸紙筒×3、黑板寫節點定則 ΣI=0 |
| cyclo 1897 卡文迪西 | 木櫃（RoundedBox+門縫線）、第二顆備用真空球管（transmission 球）、黑板寫 r=mv/eB |
| maglock 1897 暗室 | 暗房紅燈（紅色 pointLight+燈罩）、掛簾（黑 plane 微波浪）、螢光屏綠輝染牆（綠 pointLight, ambient 級） |
| dynamo 1831 地下室 | 磚拱門框（`<Instances>` 磚塊沿拱排列）、吊掛鐵鏈（torus 鏈節×8）、法拉第日記攤開（兩片 plane 攤開+paperTexture） |
| xfmr 1885 工廠 | 天花板蒸汽管（cylinder 折線+法蘭盤）、鉚釘鋼樑（`<Instances>` 半球鉚釘）、工廠吊燈（傘形 cone+暖光） |

## E. 想再上一層（CC0 免費模型管線，可選）

等 primitive 配方做完仍想加碼，再走 GLTF：Poly Haven / Kenney / Quaternius 皆 CC0。流程：下載 glb → 放 `public/models/` → `useGLTF('/models/xxx.glb')` + `<Suspense>`。只換三個主角級物件就好：陰極射線管、發電機、變壓器。**不要全面換**——primitive 的統一風格本身是優點，混搭高模反而碎。

---

## 優先序（效益 ÷ 工時）

1. **黑板 + 公式貼圖**（A1，每關一塊）— 年代感、教學、美術三合一，半天
2. **MeshTransmissionMaterial 三件套**（燈泡/e/m 球管/CRT 管）— 一行換真玻璃
3. **無人機 Trail + 複合機身** — hub 體感
4. **DialMeter 古董化 + BatteryPack 伏打堆** — 器材最常入鏡的兩件
5. **地板電路紋 + 星雲背景** — hub 氛圍
6. **門拱分段石碑環 + 吸入粒子** — 傳送門完全體
7. 各關 2–3 件年代道具（D 表）
8. （可選）三件 GLTF 主角物件

要我直接動工的話，建議從 1+2 開始（一次 commit 可完成）。
