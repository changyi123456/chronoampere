# 《電的時光旅人》全面優化實作紀錄

日期：2026-06-10 ｜ 已通過 `tsc -b && vite build` 全量驗證（Linux sandbox 建置成功）

## ⚠️ 你需要做的一件事

新增了依賴 `@react-three/postprocessing`，請先執行：

```powershell
cd "C:\Users\USER\OneDrive\桌面\codex\physics game\3D"
npm install
npm run dev
```

另外存檔格式升級為 `chrono-save-v2`，舊進度會自動重新開始（屬預期行為）。

---

## P0｜後製渲染管線（新檔 `src/components/Effects.tsx`）

- **Bloom**（mipmapBlur、luminanceThreshold=1）：所有 `toneMapped={false}` 的高亮物件——時光之門、發光邊條、燈絲、燭焰、儀表指示——現在會真正泛光。
- **Vignette** 四角壓暗 + 輕量 **ChromaticAberration**（intro 穿梭隧道時大幅加強，營造時空扭曲）。
- 已接入 `App.tsx` 的 Canvas。

## P0｜海龜湯提問系統 + 證據牆 + 多結局

- `script.ts` 新增 **12 題提問庫**（`SOUP_QUESTIONS`）：AMP 只回答「是／否／與真相無關」，含紅鯡魚（導師、第七張殘頁）。問題隨殘頁數解鎖，**提問額度 10 次**。
- Hub 新增「◈ 證據牆・提問」面板（`Hud.tsx`）：殘頁釘板（手寫便條傾斜質感）+ 提問區 + 問答記錄。
- 結局四選項改寫為**等長**（防「最長的就是答案」），新增 C 選項導師紅鯡魚。
- **多結局**：誤判 ≥2 次觸發「時間軸劣化」壞結局插敘；結算畫面附殘頁與問答回顧。
- 每間實驗室桌上新增**實體神秘紙條道具**（可點擊檢視，取得殘頁前後對話不同）——環境敘事直接服務 bootstrap 悖論。

## P0｜六關年代化場景（`theme.ts` ERAS + `RoomShell.tsx` 重寫）

資料驅動的年代主題：背景／霧／牆面／桌面／主光色溫／窗外光／燭光逐關不同。

| 關卡 | 年代場景 | 氛圍 |
|---|---|---|
| lamp | 1827 科隆・歐姆書房 | 燭光暖調、冬夜窗 |
| split | 1845 柯尼斯堡・書齋 | 冷冽晨光 |
| cyclo | 1897 劍橋・卡文迪西 | 暗室，束光是主角 |
| maglock | 1897 斯特拉斯堡・暗室 | 綠螢光屏照亮房間 |
| dynamo | 1831 倫敦・皇家研究院地下室 | 油燈磚拱 |
| xfmr | 1885 布達佩斯・岡茨工廠 | 工業黃昏 |

進關時顯示**電影感年代字卡**（大字年份淡入停留淡出，`index.css` keyframes）。

## P1｜物理真實性升級（`physics.ts` 重寫）

- **燈絲 R(T) 溫度相依**（鎢絲 α=0.004/K）：冷開機湧浪電流 → 升溫後回落。讀數明示「歐姆定律僅在定溫成立」。過關判定改用穩態自洽解（定點迭代）。
- **並聯關納入電池內阻**（與串聯關同一顆電池）：V_bus = ε − r·I_total，讀數顯示內阻壓降。
- **一階 ODE 全面改解析指數更新**（`relax()`）：RL、燈絲熱、保險絲熱零截斷誤差、永不發散。
- **e/m 關 COV 探究機制**：半徑讀值含 ±2% 不確定度；新增「📋 記錄一組數據」按鈕，須在不同 (V,I) 下記錄 ≥3 組、平均 e/m 落在理論值 ±10% 才過關（新增第三過關條件）。數據記錄持久化。
- **CRT 湯姆森平衡法**：兩場抵消 y≈0 時，讀數即時顯示「v = E/B 測得值 vs 理論值」。
- **發電機雙曲線**：示波器同屏 ε(t) 與 φ(t)（×8），直接看見 90° 相位差（「磁通為零時電動勢最大」迷思破解）。
- **變壓器負載**：固定 6Ω 負載，讀數顯示 I1/I2 電流比與功率守恆；註明波形已放慢 75 倍。

## P1｜手感與視覺

- **無人機慣性移動**：二階阻尼加減速、朝移動方向轉向、轉彎側傾（banking ~14°）、依速度前傾。
- **傳送門 shader 漩渦**：以單面 ShaderMaterial（極座標螺旋 + value noise + 邊緣亮環，HDR 輸出餵 Bloom）取代三個旋轉 torus。
- **旋鈕/滑桿刻度音**：`audio.tick()`（60ms 節流的機械喀聲）。

## 變更檔案清單

新增：`components/Effects.tsx`
重寫：`game/physics.ts`、`game/useSolve.ts`、`store/store.tsx`、`theme.ts`、`components/RoomShell.tsx`
修改：`App.tsx`、`components/Hud.tsx`、`components/HubScene.tsx`、`components/lab.tsx`、`game/audio.ts`、`story/script.ts`、`rooms/*.tsx`、`index.css`、`package.json`

## 尚未實作（評估報告中的 P2 後續）

主角級物件 GLTF/玻璃透射材質、Html 標籤全面 diegetic 化、分層 BGM、操作 log 下載（研究數據）、行動端效能分級（PerformanceMonitor）。
